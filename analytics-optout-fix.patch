diff --git a/assets/js/nexus-db.js b/assets/js/nexus-db.js
--- a/assets/js/nexus-db.js
+++ b/assets/js/nexus-db.js
@@ -3580,9 +3580,15 @@
 }
 
 function analyticsTrackingDisabled() {
-  return navigator.globalPrivacyControl === true ||
-    String(navigator.doNotTrack || "") === "1" ||
-    localStorage.getItem("nexus_analytics_opt_out") === "1";
+  if (navigator.globalPrivacyControl === true || String(navigator.doNotTrack || "") === "1") return true;
+  try {
+    return localStorage.getItem("nexus_analytics_opt_out") === "1";
+  } catch {
+    return false;
+  }
 }

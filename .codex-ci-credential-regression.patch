diff --git a/scripts/credential-integrity-regression.cjs b/scripts/credential-integrity-regression.cjs
--- a/scripts/credential-integrity-regression.cjs
+++ b/scripts/credential-integrity-regression.cjs
@@ -2,5 +2,6 @@ const fs = require("node:fs");
 const path = require("node:path");
 
 const root = path.resolve(__dirname, "..");
-const read = (relativePath) => fs.readFileSync(path.join(root, relativePath), "utf8");
+const read = (relativePath) =>
+  fs.readFileSync(path.join(root, relativePath), "utf8").replaceAll("\r\n", "\n");
 const assert = (condition, message) => {
